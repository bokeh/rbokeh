#' @param line_color color to use to stroke lines with - a hex code (with no alpha) or any of the 147 named CSS colors, e.g 'green', 'indigo'
#' @param line_width stroke width in units of pixels
#' @param line_alpha transparency value between 0 (transparent) and 1 (opaque)

