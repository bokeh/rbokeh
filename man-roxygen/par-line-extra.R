#' @param line_join how path segments should be joined together 'miter' 'round' 'bevel'
#' @param line_cap - how path segments should be terminated 'butt' 'round' 'square'
#' @param line_dash array of integer pixel distances that describe the on-off pattern of dashing to use
#' @param line_dash_offset the distance in pixels into the line_dash that the pattern should start from
