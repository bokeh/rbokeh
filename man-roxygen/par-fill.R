#' @param fill_color color to use to fill the glyph with - a hex code (with no alpha) or any of the 147 named CSS colors, e.g 'green', 'indigo'
#' @param fill_alpha transparency value between 0 (transparent) and 1 (opaque)
